import 'package:flutter/material.dart';

class ExpenseAdvancesTab extends StatelessWidget {
  const ExpenseAdvancesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Advances Coming Soon'),
    );
  }
}
