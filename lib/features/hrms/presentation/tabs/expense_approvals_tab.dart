import 'package:flutter/material.dart';

class ExpenseApprovalsTab extends StatelessWidget {
  const ExpenseApprovalsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Approvals Coming Soon'),
    );
  }
}
