import 'package:flutter/material.dart';

class ExpenseBatchProcessingTab extends StatelessWidget {
  const ExpenseBatchProcessingTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Batch Processing Coming Soon'),
    );
  }
}
