import 'package:flutter/material.dart';

class ExpenseClaimsDashboardTab extends StatelessWidget {
  const ExpenseClaimsDashboardTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Dashboard Analytics Coming Soon'),
    );
  }
}
