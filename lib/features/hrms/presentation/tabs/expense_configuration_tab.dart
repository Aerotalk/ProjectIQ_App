import 'package:flutter/material.dart';

class ExpenseConfigurationTab extends StatelessWidget {
  const ExpenseConfigurationTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Configuration Coming Soon'),
    );
  }
}
